{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach (Remote Debug)",
            "type": "python",
            "request": "attach",
             "localRoot": "${workspaceRoot}/site/",
            "remoteRoot": "/var/www/",
            "port": 3000,
            "secret": "dev",
            "host": "localhost"
        },
        {
            "name": "Attach admin (Remote Debug)",
            "type": "python",
            "request": "attach",
            "localRoot": "${workspaceRoot}/site/",
            "remoteRoot": "/var/www/",
            "port": 3001,
            "secret": "dev",
            "host": "localhost"
        }
    ]
}